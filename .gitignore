node_modules/
client/.cache/
# 只排除本仓库自己的构建输出，不要用通配的 `dist/`：那会连
# bundled-plugins/*/dist 一起吞掉，而那些是随包分发插件的运行实体
# (chatcut 的 .mcp.json 直接指向 dist/server/index.mjs)，漏提交的话
# fresh clone 打出的包里插件装上即坏且全程不报错。
# 另注：对已被排除目录内的 `!` 例外无效(git 不进入被排除目录)，
# 只能在源头把规则写精确。.claude/hooks 曾踩过同一个坑。
/client/dist/
/server/dist/
.env
.env.*
!.env.example
*.db
*.db-*
*.sqlite
*.sqlite3
credentials
credentials.*
*.pem
*.key
*.p12
*.pfx
*.log
.DS_Store
Thumbs.db
target/
client/src-tauri/target/
client/src-tauri/gen/
client/src-tauri/binaries/
client/src-tauri/server-dist/
# .claude 同时装着两类东西，必须分开对待：
#   框架文件（hooks / skills / agents / 指令文档）—— 是项目基础设施，必须入库
#   运行时数据（attachments / evolution / workflows / 本机覆盖）—— 含用户原话，绝不入库
#
# 此前一刀切只放行 settings.json，后果有两层：
#   1. 新 clone 的机器上，settings.json 注册的 7 个 hook 全部指向不存在的脚本
#   2. 更糟：client/scripts/prepare-sidecar.mjs 会把 .claude 整个打进安装包，
#      作为「+ 添加项目」的初始化模板。在 fresh clone 上打包，模板里就只剩
#      settings.json —— 用户新建的每个项目都会拿到一份指向空气的 hook 配置。
#      而本机因为文件还在（未跟踪但未删），打出来的包是好的，故一直没暴露。
.claude/*
!.claude/settings.json
!.claude/CLAUDE.md
!.claude/EVOLUTION.md
!.claude/使用说明.md
!.claude/agents/
!.claude/hooks/
!.claude/skills/
.engram/
**/.engram/

# 构建/发布产物(含安装包,体积大且可重新生成)
release/
*.exe

# 与项目无关的本地文件
duojiao-yutou-recipe.png
*.tsbuildinfo

# 本地调试工具产物
.playwright-cli/

# 个股研报与日报：本地个人资料，不进公开仓库
# （本仓库是 public 且装了 commit 自动推送的 hook，漏配就会被推上公网）
research/
daily/
