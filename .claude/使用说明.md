# 废才框架 · Claude Code 版 安装说明

1. 把本包里的 claude 文件夹重命名为 .claude（前面加一个点）。
2. 把重命名后的 .claude 放进你的项目根目录。
3. 重命名后文件夹会变成隐藏状态，这是正常的。macOS 在访达里按 Cmd+Shift+. 可以显示隐藏文件。
4. 在项目根目录启动 Claude Code 即可，主控文件是 .claude/CLAUDE.md。
