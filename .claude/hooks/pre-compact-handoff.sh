#!/bin/bash
# Hook: PreCompact
# 上下文压缩前注入交接指令：压缩摘要必须保留任务状态，压缩后立即落盘 HANDOFF.md。
cat > /dev/null  # 排干 stdin

cat <<'EOF'
[上下文交接指令]
压缩摘要必须完整保留：当前任务与目标、已完成步骤及证据、下一步计划、涉及的关键文件路径、已排除的死路和踩过的坑、用户尚未拍板的待办。
压缩完成后第一件事：把上述状态写入项目根目录 HANDOFF.md（首行标注「状态: 进行中」），它是跨会话交接的单一真相源；任务全部完结时把状态改为「状态: 已完结」。
EOF
exit 0
