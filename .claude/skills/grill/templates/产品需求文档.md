# {{产品名}} —— 产品需求文档

> 状态：{{拷问中 / 需求全部确认 / 开发中}}。只有"已确认"的需求才进入开发。
> 用户原话原样引用、带日期；Agent 定的默认标"Agent 默认，待确认"。每节末尾标"示例（虚构）"的那一行是给你看格式的，写真内容时删掉。

## 1. 产品概述
（五层漏斗的结论，一节一层。写事实和结论，不写问答过程。）

- 一句话：{{它是什么，给谁，解决什么}}
  示例（虚构）：给独立播客主用的本地剪辑助手，把两小时素材剪成十分钟的精华。
- 目标用户：{{一类具体的人，不是"所有人"}}
- 核心任务：{{它被雇来干什么，按重要性排；现在是怎么应付的、痛点在哪}}
- 成功指标：{{一个月后看什么；基线多少、目标多少}}
  示例（虚构）：现在每期剪辑 6 小时，目标 1 小时以内。
- 使用场景：{{谁、什么时候、什么情境下用；只记影响设计的时间点}}
- 主流程与人工介入点：{{主干一行；哪几步要用户亲手确认}}
- 产品形态：{{网站 / 桌面软件 / 手机 App / 命令行工具；定形态时说清的代价}}
- AI 用在哪：{{哪个功能用、失败了怎么办、大概花多少钱；不用就写"不用"}}
- 质量标准与验收方式：{{什么算做完；用户怎么验}}
- 不能接受的失败：{{列出来；第三方服务故障的写成"能发现、能告警、能重试、能降级"}}

## 2. 原始需求描述（{{日期}}，原文保留）
> {{用户第一次说的话，一字不改}}

## 3. 需求清单
初稿 = 从描述里抽出来还没问；已确认 = 用户认过；六列全满 = 做完。没有界面的产品"设计落点"填 N/A，不发布的"已发布"填 N/A。

| 编号 | 需求 | 一句话 | 已确认 | 设计落点 | 在哪个阶段 | 已实现 | 已验证 | 已发布 |
|---|---|---|---|---|---|---|---|---|
| R-001 | {{名字}} | {{它管什么}} | ○ | | | | | |
| 示例（虚构）R-001 | 导入素材 | 拖一个文件夹进来，音视频都能识别 | ✓ | 首页 · 拖放区 | 第 1 阶段 | ✓ | ✓ | ○ |

## 4. 需求详述
（确认一条补一块。字段固定，不省。）

### R-001 {{名字}}（已确认，{{日期}}）
- 描述：{{}}
- 触发场景：{{什么时候用到；上一次是什么时候}}
- 现状：{{没有它怎么做的、为什么不行}}
- 输入：{{要什么、从哪来、谁给}}
- 输出：{{产出什么、什么形态、存哪、给谁}}
- 验收条件：
  - Given {{前提}}，When {{用户做什么}}，Then {{看到什么}}
- 边界：{{什么不算它的事}}
- 异常处理：{{空、重复、出错、超时、量特别大或特别小时怎么办}}
- 优先级：{{必备 / 有余力再做}}
- 来源：{{真实痛点，还是"别人有"}}
- 用户确认：{{日期，原话}}
- 修订（{{日期}}，用户："{{原话}}"）：{{改了什么}}

示例（虚构）：
### R-001 导入素材（已确认，2026-01-08）
- 描述：把一个文件夹拖进来，里面的音频和视频都列出来，按拍摄时间排。
- 触发场景：每期录完，用户把存储卡里的文件夹拖进来。上一次：上周三，拖了 47 个文件。
- 现状：手动一个个打开看，挑错过好几次。
- 输入：一个文件夹；里面可能混着非媒体文件。
- 输出：素材列表，每条带时长和拍摄时间。
- 验收条件：Given 文件夹里有 3 个视频 1 个文本，When 拖进来，Then 列出 3 条按时间排，文本文件不出现。
- 边界：不转码、不剪。
- 异常处理：空文件夹提示"没有音视频"；同名文件按路径区分；1000 个文件也要能列。
- 优先级：必备。
- 来源：真实痛点。
- 用户确认：是，2026-01-08。

## 5. 不做的（N-xxx）
| 编号 | 不做的事 | 依据 |
|---|---|---|
| N-001 | {{}} | {{用户定的 / 默认}} |
| 示例（虚构）N-001 | 云端同步 | 用户："只在这台电脑上用" |

## 6. 技术约束
- {{运行环境、形态、要接的外部服务、有 AI 的话它自身需要的能力}}

## 7. 待确认（Q-xxx）
| 编号 | 问题 | 挡不挡开发 | 状态 |
|---|---|---|---|
| Q-001 | {{}} | {{挡 / 不挡}} | {{待答 / Agent 默认待确认 / 已答（日期，原话）}} |
