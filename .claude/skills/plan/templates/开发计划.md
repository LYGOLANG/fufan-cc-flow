# {{产品名}} 开发计划

> 一页纸：开发前定下的约定、阶段表、没有 key 时怎么做、风险。做到哪个阶段更新哪一行。标"示例（虚构）"的行是给你看格式的，写真内容时删掉。

## 0. 开发前定下的约定
- 代码目录：{{}}
- 形态：{{网站 / 桌面软件 / 手机 App / 命令行工具}}
- 技术栈：{{名字 + 版本，哪天查的}}
- 数据放哪：{{}}
- 外部服务与 key：{{}}
- git：{{仓库地址，第 0 阶段建好后填；没有 GitHub 账号就先只在本机提交}}
- 运行时规矩：{{怎么启动、怎么停止、运行中不能动什么}}
- 示例（虚构）：代码在 `clipper/`；桌面软件；Node 22 + Electron 33，pnpm，2026-01-09 查的；数据在 `~/Clipper/`；不用 AI；仓库 github.com/x/clipper 私有；预览开着时别改主进程文件。

## 1. 阶段
| 阶段 | 走通什么 | 验收自 | 验证结果 | 状态 |
|---|---|---|---|---|
| 0 | 搭骨架：{{}} | 技术约束 | | 待做 |
| 1 | {{}} | R-xxx | | 待做 |
| 示例（虚构）1 | 拖文件夹进来 → 列出素材 → 首页能看到 | R-001 | 审过：2 条修了；真用一遍过；用户点过 | 完成 2026-01-10 |

每个阶段的完成标准就是它验收自的那几条需求的 Given / When / Then，不另写一套。

## 2. 没有 key 的时候怎么做
- {{哪些阶段用固定数据代替；key 到了换哪里}}

## 3. 风险与未定项
- {{没验证的技术、要花钱的、用户先不拍板的、Agent 默认待确认的}}
