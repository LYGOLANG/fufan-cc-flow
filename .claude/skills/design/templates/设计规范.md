# {{产品名}} —— 设计规范

> 原型和真实界面只用这里的东西，不自行发明样式。每个值写清从哪来。标"示例（虚构）"的行是给你看格式的，写真内容时删掉。

## 1. 原则
（三到五条能指导取舍的话，不是"好看"。）
- {{}}
- 示例（虚构）：层级靠明度和字重，不靠颜色；颜色只给状态和强调。

## 2. 颜色
（只做一套主题就只填一列。）

| 用途 | 浅色 | 深色 | 从哪来 |
|---|---|---|---|
| 页面底 | | | |
| 主文字 | | | |
| 次要文字 | | | |
| 边框 / 分割线 | | | |
| 强调（选中、链接） | | | |
| 状态（成功 / 警告 / 错误） | | | |
| 示例（虚构）主文字 | #1C2024 | #EDEEF0 | 2026-01-09 读自样板站的计算样式 |

## 3. 字
| 用途 | 字号 / 行高 / 字重 |
|---|---|
| 页面标题 | |
| 正文 | |
| 辅助说明 | |
| 示例（虚构）正文 | 15 / 24 / 400 |

## 4. 间距、圆角、边框、投影
- 间距阶梯：{{}}
- 圆角：{{}}
- 边框与投影：{{}}
- 示例（虚构）：间距 4、8、12、16、24、32；卡片圆角 8，按钮胶囊；投影只给弹层。

## 5. 布局
- 壳：{{几栏、各多宽、边距}}
- 对齐线：{{页头基线、两条竖线、底边；交付前量}}
- 自适应：{{断点；窄屏时怎么退让——换行 → 收起 → 隐藏；不超框、不压框、不堆叠}}

## 6. 交互
- 键盘：{{}}
- 悬停 / 按下 / 选中 / 禁用：{{}}
- 加载与反馈：{{短于 300 毫秒不显示；成功用轻提示；失败原地说错在哪、怎么办}}
- 确认：{{只有不可逆的操作弹确认，其余可撤销}}
- 空状态：{{一句话说这里会出现什么，加一个动作}}
- 动效：{{}}

## 7. 组件清单
（写新页面先查这张表，有就复用；新组件先加进表再写。）

| 组件 | 长什么样 | 怎么用 | 别怎么用 |
|---|---|---|---|
| {{}} | | | |
| 示例（虚构）主按钮 | 黑实底胶囊，高 32 | 一屏一个最重要的动作 | 一屏两个；做成文字 |

## 8. 文案
- 按钮是动词；错误说清错在哪、怎么办；{{语气}}

## 9. 来源
- {{读了哪个产品的哪个页面、哪天}}
